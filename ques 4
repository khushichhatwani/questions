Write R commands to do the following: a. Create a sample STUDENT table in the MySQL database. b. Insert 5 rows into the STUDENT table. c. Display all the information stored in STUDENT table. d. Delete all the rows from STUDENT table. e. Drop the STUDENT table

#a) 
library("RMySQL")
con<-dbConnect(MySQL(),username="root",password="",host="localhost",dbname="nitika_20059570006")
q="CREATE TABLE STUDENT (Student_id int(5) Primary key,Student_Name varchar(30));"
dbGetQuery(con,q)

#b)
dbSendQuery(con,"insert into student(Student_id,Student_Name)values(1,'Nitika')")

#c)
dbGetQuery(con,"select * from student")

#d)
dbGetQuery(con,"delete from student")

#e)
dbSendQuery(con,'drop table student')
